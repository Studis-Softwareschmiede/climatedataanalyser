# Vorlage — von new-project nach .github/workflows/build.yml kopiert.
# Baut bei Push auf main ein Image und pusht nach ghcr.io — via eingebautem GITHUB_TOKEN
# (kein Push-Secret nötig). Typ-Spezifika stecken im Dockerfile.
# Security-Gate: secret-scan (gitleaks) MUSS grün sein, bevor das Image gebaut/gepusht wird.
name: build
on:
  push:
    # main = new-project default; master = Fork-Default dieses Org-Forks
    branches: [main, master]
    paths-ignore:
      - 'board/**'
      - 'docs/**'
      - '**/*.md'
      - '.claude/**'
permissions:
  contents: read
  packages: write
# Verhindert das :latest-Race: laufen zwei Pushes auf master kurz nacheinander (z.B. zwei
# schnell aufeinanderfolgende Merges), würde sonst der zuletzt fertige Build :latest
# überschreiben — auch wenn er den älteren Commit baut. cancel-in-progress bricht den
# älteren Build ab → :latest stammt immer vom jüngsten master-Stand.
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true
jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Secret-Scan (gitleaks)
        run: docker run --rm -v "$PWD:/repo" ghcr.io/gitleaks/gitleaks:latest detect --source=/repo --no-git --redact -v
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run tests
        run: mvn -B -ntp -pl climatedataanalyser-api -am test
  image:
    needs: [secret-scan, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}   # wird automatisch klein geschrieben
          tags: |
            type=raw,value=latest
            type=sha
      - uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
# Härtung später (train/teamLeader): Actions auf SHA pinnen.
